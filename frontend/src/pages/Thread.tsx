import { Box } from "@mui/material";

const Thread = () => {
	return (
		<Box
			sx={{
				flexGrow: 1,
				bgcolor: "background.default",
				p: 3,
				minHeight: "100%",
			}}
		></Box>
	);
};

export default Thread;
